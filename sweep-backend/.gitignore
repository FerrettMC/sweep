node_modules
# Keep environment variables out of version control
.env

/src/generated/prisma

# Build output — rebuilt on every deploy, never committed.
dist/

# Dev-project credentials for tests. Holds a service_role key.
.env.test
